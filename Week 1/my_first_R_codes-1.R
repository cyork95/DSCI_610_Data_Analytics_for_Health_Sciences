x <- c(2, 3, 4, 5) # A set of four whole numbers

fruit <- c("apple", "orange", "banana", "pear") # A set of four character values

gender <- c("male", "female") # A set of two character values

TF <- c("TRUE", "FALSE") # A set of two logical values

y <- 2 * x

# Another example: just a comment

ht <- c(1.2, 1.3, 1.5, 1.7) # height in meter
wt <- c(50, 65, 76, 65) # weight in kg

bmi <- wt / ht^2  # weight in kg/ (height in meter)^2

bmi  # view the bmi values

hist(bmi) # create a histogram of four bmi values
